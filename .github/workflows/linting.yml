# Note on running admin tasks that would use `typer-invoke`:
#   pip install typer-invoke
#   inv some task
#
# Since this project *is* `typer-invoke`, we run it directly:
#   python -m src.typer_invoke.invoke some task

name: Linting
# Linting tools use `pyproject.toml` for config.

on:
  - pull_request

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Create virtual environment
        run: uv venv

      - name: Install requirements
        run: |
          uv pip install typer
          python -m src.typer_invoke.invoke pip install dev

      - name: Linting
        run: python -m src.typer_invoke.invoke lint all --check
